{
  "name": "api-sync",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "npm run clean && tsc -w",
    "prepublish": "npm run clean && tsc"
  },
  "bin": "bin/api-sync",
  "author": "cheerwu",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.4",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "commander": "^9.4.1",
    "fs-extra": "^10.1.0"
  }
}
